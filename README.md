# IELTS Listening Platform

Полнофункциональная платформа для проведения IELTS Listening тестов с автоматическим оцениванием, безопасным воспроизведением аудио и админ-панелью для создания тестов.

## 🎯 Основные функции

### Для студентов:
- ✅ Прохождение IELTS Listening тестов в реальном времени
- ✅ Аудиоплеер с контролем воспроизведения
- ✅ Таймер (30 минут как в реальном IELTS)
- ✅ Все типы вопросов IELTS: Completion, Multiple Choice, Matching, True/False, Short Answer, Sentence Completion, Flow Chart, Table Completion
- ✅ Автоматическое сохранение ответов
- ✅ Автогрейдинг с конвертацией в IELTS band score (0-9)
- ✅ Детальные результаты с анализом ответов
- ✅ Правило "один сеанс" - нельзя прерывать и возобновлять тест

### Для админов:
- ✅ Полный CRUD для создания и редактирования тестов
- ✅ Drag-and-drop для изменения порядка вопросов
- ✅ Загрузка аудио файлов с валидацией
- ✅ Клонирование тестов
- ✅ Активация/деактивация тестов
- ✅ Просмотр результатов студентов
- ✅ Безопасное хранение аудио файлов

## 🏗️ Архитектура

### Backend (Django REST Framework)
- **Модели**: ListeningTest, ListeningPart, ListeningQuestion, ListeningAnswerOption, ListeningTestSession
- **API**: RESTful endpoints для всех операций
- **Аутентификация**: Firebase Authentication
- **Автогрейдинг**: Алгоритм конвертации в IELTS band score
- **Безопасность**: Валидация файлов, проверка прав доступа

### Frontend (React)
- **Компоненты**: ListeningTestPlayer, ListeningTestBuilder
- **Стилизация**: Tailwind CSS
- **Состояние**: React Hooks
- **Навигация**: React Router
- **Drag & Drop**: react-beautiful-dnd

## 🚀 Установка и настройка

### Требования
- Python 3.9+
- Node.js 16+
- PostgreSQL (рекомендуется) или SQLite
- Firebase проект

### Backend установка

1. **Клонируйте репозиторий**
```bash
git clone <repository-url>
cd ielts-platform/backend
```

2. **Создайте виртуальное окружение**
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
venv\Scripts\activate     # Windows
```

3. **Установите зависимости**
```bash
pip install -r requirements.txt
```

4. **Настройте переменные окружения**
```bash
cp .env.example .env
# Отредактируйте .env файл
```

5. **Настройте базу данных**
```bash
python manage.py makemigrations
python manage.py migrate
```

6. **Создайте суперпользователя**
```bash
python manage.py createsuperuser
```

7. **Запустите сервер**
```bash
python manage.py runserver
```

### Frontend установка

1. **Перейдите в папку frontend**
```bash
cd ../frontend
```

2. **Установите зависимости**
```bash
npm install
```

3. **Настройте Firebase**
```bash
# Создайте firebase-config.js на основе вашего Firebase проекта
```

4. **Запустите приложение**
```bash
npm start
```

## 📚 Использование

### Для студентов

1. **Войдите в систему** через Firebase Authentication
2. **Выберите тест** из списка доступных
3. **Начните тест** - создается сессия с таймером
4. **Прослушайте аудио** и ответьте на вопросы
5. **Отправьте тест** - получаете автоматический результат

### Для админов

1. **Войдите как админ** (роль 'admin' в базе данных)
2. **Создайте новый тест** или отредактируйте существующий
3. **Добавьте части** (Part 1, Part 2, Part 3, Part 4)
4. **Загрузите аудио** для каждой части
5. **Создайте вопросы** с правильными ответами
6. **Активируйте тест** для студентов

## 🔌 API Endpoints

### Тесты
- `GET /api/listening-tests/` - список тестов
- `GET /api/listening-tests/{id}/` - детали теста
- `POST /api/listening-tests/` - создать тест (admin)
- `PUT /api/listening-tests/{id}/` - обновить тест (admin)
- `DELETE /api/listening-tests/{id}/` - удалить тест (admin)

### Сессии
- `POST /api/listening-tests/{id}/start/` - начать сессию
- `PATCH /api/listening-sessions/{id}/sync/` - синхронизировать ответы
- `POST /api/listening-sessions/{id}/submit/` - отправить тест
- `GET /api/listening-sessions/{id}/result/` - получить результат

### Админ функции
- `POST /api/listening-tests/{id}/activate/` - активировать тест
- `POST /api/listening-tests/{id}/deactivate/` - деактивировать тест
- `POST /api/listening-tests/{id}/clone/` - клонировать тест
- `POST /api/admin/audio/upload/` - загрузить аудио
- `GET /api/admin/check/` - проверить админ права

## 🎨 Типы вопросов

### Completion
- Заполнение пропусков в тексте
- Один или несколько правильных ответов

### Multiple Choice
- Выбор одного правильного ответа из вариантов
- Поддержка A, B, C, D меток

### Matching
- Сопоставление элементов из двух списков
- Поддержка множественных соответствий

### True/False
- Определение истинности утверждений
- Варианты: True, False, Not Given

### Short Answer
- Краткие ответы на вопросы
- Ограничение по количеству слов

### Sentence Completion
- Завершение предложений
- Грамматическая корректность

### Flow Chart
- Заполнение блок-схемы
- Логическая последовательность

### Table Completion
- Заполнение таблицы
- Структурированные данные

## 🔒 Безопасность

- **Firebase Authentication** для аутентификации
- **Проверка прав доступа** для админ функций
- **Валидация файлов** при загрузке аудио
- **Безопасное хранение** аудио файлов
- **Защита от списывания** - один сеанс, таймер, запрет пауз

## 📊 Автогрейдинг

Система автоматически оценивает тесты по алгоритму IELTS:

- **Подсчет правильных ответов** по всем частям
- **Конвертация в процент** от общего количества
- **Перевод в IELTS band score** (0-9):
  - 90%+ = 9.0
  - 85-89% = 8.5
  - 80-84% = 8.0
  - 75-79% = 7.5
  - 70-74% = 7.0
  - 65-69% = 6.5
  - 60-64% = 6.0
  - 55-59% = 5.5
  - 50-54% = 5.0
  - 45-49% = 4.5
  - 40-44% = 4.0
  - <40% = 0.0

## 🛠️ Разработка

### Структура проекта
```
ielts-platform/
├── backend/
│   ├── core/
│   │   ├── models.py          # Модели данных
│   │   ├── views.py           # API endpoints
│   │   ├── serializers.py     # Сериализаторы
│   │   └── urls.py           # URL маршруты
│   ├── manage.py
│   └── requirements.txt
└── frontend/
    ├── src/
    │   ├── components/
    │   │   ├── ListeningTestPlayer.jsx
    │   │   └── ListeningTestBuilder.jsx
    │   ├── pages/
    │   │   ├── ListeningTestPage.js
    │   │   └── AdminListeningTestBuilderPage.js
    │   └── App.js
    └── package.json
```

### Добавление новых типов вопросов

1. Добавьте тип в `questionTypes` в `ListeningTestBuilder.jsx`
2. Добавьте логику рендеринга в `renderQuestion` в `ListeningTestPlayer.jsx`
3. Обновите валидацию в сериализаторах
4. Добавьте тесты

## 🧪 Тестирование

```bash
# Backend тесты
cd backend
python manage.py test

# Frontend тесты
cd frontend
npm test
```

## 📝 Лицензия

MIT License

## 🤝 Вклад в проект

1. Fork репозитория
2. Создайте feature branch
3. Внесите изменения
4. Добавьте тесты
5. Создайте Pull Request

## 📞 Поддержка

Для вопросов и поддержки создайте Issue в репозитории. 