# 📚 Руководство по переносу IELTS тестов между серверами

Данное руководство поможет вам перенести готовые Listening и Reading тесты с одного сервера на другой без потери данных и медиа файлов.

## 🎯 Что можно переносить

- ✅ **Listening тесты** - полная структура включая аудио файлы
- ✅ **Reading тесты** - полная структура включая изображения  
- ✅ **Медиа файлы** - аудио, изображения, все связанные файлы
- ✅ **Все связи** - части, вопросы, варианты ответов
- ✅ **Метаданные** - настройки скоринга, типы вопросов, etc.

## 📋 Подготовка

### На исходном сервере:
1. Убедитесь что Django окружение активно
2. Перейдите в директорию `backend/`
3. Убедитесь что все нужные тесты активны (`is_active=True`)

### На целевом сервере:
1. Установите и настройте Django проект
2. Выполните миграции: `python manage.py migrate`
3. Убедитесь что медиа директории существуют

## 🚀 Экспорт тестов (исходный сервер)

### Экспорт всех активных тестов
```bash
cd backend/
    python export_tests.py --all -o my_tests_export.zip
```

### Экспорт только Listening тестов
```bash
python export_tests.py --listening -o listening_tests.zip
```

### Экспорт только Reading тестов
```bash
python export_tests.py --reading -o reading_tests.zip
```

### Экспорт конкретных тестов по ID
```bash
# Конкретные Listening тесты
python export_tests.py --listening --listening-ids 1,3,5 -o selected_listening.zip

# Конкретные Reading тесты  
python export_tests.py --reading --reading-ids 2,4,6 -o selected_reading.zip

# Микс тестов
python export_tests.py --listening-ids 1,2 --reading-ids 3,4 -o mixed_tests.zip
```

### Результат экспорта
После выполнения вы получите ZIP архив содержащий:
- `test_data.json` - данные тестов в JSON формате
- `media/` - папка с медиа файлами
  - `listening_audio/` - аудио файлы
  - `listening_images/` - изображения для Listening
  - `reading_images/` - изображения для Reading
  - `secure_audio/` - защищенные аудио файлы

## 🔍 Инспекция архива

Перед импортом можно проверить содержимое архива:

```bash
python inspect_export.py my_tests_export.zip
```

Это покажет:
- ✅ Список всех тестов с деталями
- ✅ Количество частей и вопросов в каждом тесте
- ✅ Медиа файлы и их группировку
- ✅ Метаданные экспорта
- ⚠️ Отсутствующие файлы (если есть)

## 📥 Импорт тестов (целевой сервер)

### Перенос архива
```bash
# Скопируйте архив на целевой сервер
scp my_tests_export.zip user@target-server:/path/to/project/backend/
```

### Предварительный просмотр (DRY RUN)
```bash
cd backend/
python import_tests.py my_tests_export.zip --dry-run
```

Это покажет что будет импортировано БЕЗ реального импорта.

### Импорт новых тестов
```bash
python import_tests.py my_tests_export.zip
```

Существующие тесты с такими же названиями будут пропущены.

### Импорт с перезаписью
```bash
python import_tests.py my_tests_export.zip --overwrite
```

⚠️ **ВНИМАНИЕ**: Это перезапишет существующие тесты с такими же названиями!

## 📊 Что происходит при импорте

1. **Проверка конфликтов** - поиск тестов с такими же названиями
2. **Копирование медиа** - все файлы копируются в правильные папки
3. **Создание записей БД** - тесты создаются в правильном порядке:
   - Тест → Части → Вопросы → Варианты ответов
4. **Восстановление связей** - все Foreign Key связи восстанавливаются
5. **Отчет** - показывается статистика импорта

## ❗ Возможные проблемы и решения

### Конфликт названий тестов
```
⏭️ Пропущен (уже существует): IELTS Listening Test 1
```
**Решение**: Используйте `--overwrite` или переименуйте тесты перед экспортом.

### Отсутствующие медиа файлы
```
⚠️ В экспорте отсутствовали файлы: 3
   - listening_audio/missing_file.mp3
```
**Решение**: Найдите отсутствующие файлы на исходном сервере и пересоздайте экспорт.

### Ошибки базы данных
```
❌ Ошибка импорта: IntegrityError...
```
**Решение**: Проверьте миграции и совместимость моделей между серверами.

### Недостаточно места
**Решение**: Освободите место или импортируйте тесты по частям.

## 🛡️ Безопасность и бэкапы

### Перед импортом на продакшен:
1. **Создайте бэкап БД**:
   ```bash
   pg_dump your_db > backup_before_import.sql
   ```

2. **Создайте бэкап медиа**:
   ```bash
   tar -czf media_backup.tar.gz media/
   ```

3. **Тестируйте на стейджинге** перед продакшеном

### После импорта:
1. **Проверьте тесты** в админке
2. **Протестируйте прохождение** тестов студентами
3. **Проверьте медиа файлы** в браузере

## 📈 Оптимизация

### Для больших экспортов:
- Экспортируйте по частям (по 10-20 тестов)
- Сжимайте медиа файлы перед экспортом
- Используйте быстрый интернет для передачи

### Для регулярных переносов:
- Создайте скрипт автоматизации
- Настройте cron для регулярных экспортов
- Используйте инкрементальные экспорты

## 🧪 Примеры использования

### Сценарий 1: Полный перенос всех тестов
```bash
# На исходном сервере
python export_tests.py --all -o full_export.zip

# Перенос файла
scp full_export.zip user@new-server:/path/to/backend/

# На новом сервере
python inspect_export.py full_export.zip  # проверяем
python import_tests.py full_export.zip    # импортируем
```

### Сценарий 2: Обновление конкретных тестов
```bash
# Экспорт обновленных тестов
python export_tests.py --reading-ids 5,6,7 -o updates.zip

# Импорт с перезаписью
python import_tests.py updates.zip --overwrite
```

### Сценарий 3: Миграция на новую версию
```bash
# 1. Экспорт со старого сервера
python export_tests.py --all -o migration.zip

# 2. Подготовка нового сервера
python manage.py migrate
python manage.py collectstatic

# 3. Тестовый импорт
python import_tests.py migration.zip --dry-run

# 4. Реальный импорт
python import_tests.py migration.zip
```

## 🆘 Получение помощи

Для получения справки по любому скрипту:
```bash
python export_tests.py --help
python import_tests.py --help  
python inspect_export.py --help
```

## ✅ Чеклист переноса

- [ ] Создан бэкап исходной БД
- [ ] Создан бэкап медиа файлов
- [ ] Выполнен экспорт тестов
- [ ] Проведена инспекция архива
- [ ] Выполнен DRY RUN импорта
- [ ] Архив перенесен на целевой сервер
- [ ] Выполнен реальный импорт
- [ ] Проверены тесты в админке
- [ ] Протестировано прохождение тестов
- [ ] Проверены медиа файлы
- [ ] Создан бэкап после импорта

---

🎉 **Готово!** Ваши IELTS тесты успешно перенесены на новый сервер!

