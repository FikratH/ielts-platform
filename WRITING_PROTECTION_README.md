# Writing Test Copy Protection

## Обзор

Система защиты от копирования и вставки для Writing тестов IELTS платформы предотвращает списывание и обеспечивает честность тестирования.

## Реализованные функции

### 🔒 Защита задания от копирования
- **CSS стили**: `user-select: none` для всех браузеров
- **Обработчики событий**: `onCopy`, `onSelectStart`, `onDragStart`, `onContextMenu`
- **Визуальные индикаторы**: Иконка блокировки с анимацией
- **Защита изображений**: Отключение перетаскивания и контекстного меню

### 🚫 Защита поля эссе от вставки
- **Блокировка вставки**: `onPaste` обработчик с `preventDefault()`
- **Блокировка вырезания**: `onCut` обработчик
- **Блокировка копирования**: `onCopy` обработчик
- **Визуальная индикация**: Иконка блокировки рядом с полем

### ⌨️ Глобальная защита клавиатуры
- **Ctrl/Cmd + V**: Блокировка вставки
- **Ctrl/Cmd + C**: Блокировка копирования  
- **Ctrl/Cmd + X**: Блокировка вырезания
- **F12, Ctrl+Shift+I/J/C**: Блокировка DevTools

### 🔍 Дополнительная защита
- **Обнаружение DevTools**: Мониторинг изменения размеров окна
- **Уведомления**: Toast-сообщения о попытках нарушения
- **Предупреждение о правилах**: Информационный блок с правилами

## Файлы

### Основные компоненты
- `frontend/src/pages/WritingTaskPage.js` - Основной компонент с защитой
- `frontend/src/styles/writing-protection.css` - CSS стили для защиты

### Ключевые функции

#### `showNotification(message)`
Показывает уведомление пользователю на 3 секунды

#### `handleTaskCopy(e)`
Блокирует копирование задания и показывает предупреждение

#### `handleEssayPaste(e)`
Блокирует вставку в поле эссе и показывает предупреждение

#### `handleKeyDown(e)`
Глобальный обработчик клавиатуры для блокировки горячих клавиш

## Стили

### CSS классы
- `.no-copy` - Запрет выделения текста
- `.lock-icon` - Анимация иконки блокировки
- `.animate-slide-in` - Анимация появления уведомлений
- `.notification-toast` - Стили уведомлений

### Анимации
- `slide-in` - Появление уведомления справа
- `lock-pulse` - Пульсация иконки блокировки

## Тестирование

### Проверенные сценарии
1. ✅ Копирование задания мышью
2. ✅ Копирование задания клавиатурой (Ctrl+C)
3. ✅ Вставка в поле эссе (Ctrl+V)
4. ✅ Вырезание из поля эссе (Ctrl+X)
5. ✅ Попытки через контекстное меню
6. ✅ Перетаскивание изображений
7. ✅ Попытки открыть DevTools

### Браузеры
- ✅ Chrome
- ✅ Firefox  
- ✅ Safari
- ✅ Edge

## Безопасность

### Уровни защиты
1. **CSS уровень**: `user-select: none`
2. **JavaScript уровень**: Обработчики событий
3. **Клавиатурный уровень**: Глобальные обработчики
4. **Визуальный уровень**: Индикаторы и уведомления

### Ограничения
- Защита работает только на клиентской стороне
- Опытные пользователи могут обойти через DevTools
- Рекомендуется дополнительная серверная валидация

## Будущие улучшения

### Возможные дополнения
1. **Серверная валидация**: Проверка скорости ввода текста
2. **Логирование**: Запись попыток нарушения правил
3. **Расширенная защита**: Блокировка скриншотов
4. **Анализ текста**: Обнаружение скопированного контента

### Мониторинг
- Отслеживание попыток копирования
- Статистика нарушений по пользователям
- Анализ эффективности защиты

## Использование

Система автоматически активируется при загрузке Writing теста. Никаких дополнительных настроек не требуется.

### Для разработчиков
```javascript
// Добавление защиты к новому элементу
<div 
  className="no-copy"
  onCopy={handleTaskCopy}
  onSelectStart={handleTaskSelectStart}
  style={{ userSelect: 'none' }}
>
  Защищенный контент
</div>
```

## Поддержка

При возникновении проблем проверьте:
1. Импорт CSS файла стилей
2. Корректность обработчиков событий
3. Совместимость с браузером
4. Отсутствие конфликтов с другими скриптами
